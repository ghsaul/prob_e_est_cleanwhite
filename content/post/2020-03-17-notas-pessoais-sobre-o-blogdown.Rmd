---
title: Notas pessoais sobre o Blogdown
subtitle: O que eu descobri sobre ele
description: Isso aqui é uma descrição e vai aparecer só na home page
author: Gabriel Holmer Saul
date: '2020-03-17'
categories:
  - Blog
tags:
  - cool
  - fancy
  - tutorial
image: /post/2020-03-17-notas-pessoais-sobre-o-blogdown_files/wallpaper2.png
slug: notas-pessoais-sobre-o-blogdown
---


# Notas pessoais sobre o Blogdown
###### Por Gabriel Holmer Saul

```{r libraries, message=FALSE, warning=FALSE, include=FALSE}

# Colocando algumas opcoes globais dos proximos chunks
knitr::opts_chunk$set(message = FALSE, warning = FALSE)

# Digite aqui os pacotes que voce deseja chamar
pacotes <- c(
  "blogdown"
)

# Se ele nao estiver instalado, este codigo ira instala-lo
for (i in pacotes) {
  if(!require(i, character.only = T)){install.packages(i, dependencies = T)}
}

# Chama todos os pacotes
lapply(pacotes, library, character.only = TRUE)

```

# 1.1 - Instalação do Blogdown e Hugo

```{r c1_1, eval=FALSE, include=TRUE}

install.packages('blogdown') # Instala o Blogdown
blogdown::install_hugo() # Instala o Hugo
blogdown::update_hugo() # Pode ser usado para atualizar o Hugo
# Talvez seja uma boa ideia rodar esse codigo logo depois de instalar o Hugo, só pra garantir

```

# 1.2 - Criando o site

```{r c1_2, eval=FALSE, include=TRUE}

new_site(dir="nome_do_diretorio_do_site")

```

<p align="justify"> asdasdasdasO comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando.O comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando.O comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando.O comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando.O comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando. </p>

O comando acima cria um site novo com especificações passadas nos argumentos dessa função. Para criar seu primeiro site, basta rodar o comando.

O diretorio que irá conter o site precisa não existir ou estar vazio. Se não for especificado o argumento `dir`, o comando tentará criar o site no diretorio atual, que muito provavelmente nao estara vazio. Por isso, use o argumento `dir = "nome_da_pasta_do_site"`, dentro da função.



```{r}
# 

# Debulhando um pouco mais o comando

# new_site(
#   dir = ".", # Diretorio do site
#   install_hugo = TRUE, # Instala o Hugo automaticamente se ele nao for encontrado
#   format = "toml",
#   sample = TRUE, # Adiciona algum conteudo ao site, caso ele nao tenha nada
#   theme = "yihui/hugo-lithium", # Usar o formato usuario/repositorio para baixar um tema do github, URL completa com o arquivo zip do tema ou NA para instalar o tema manualmente
#   hostname = "github.com", # Use github.com se usar um tema do github, se usar outro tipo de tema este argumento e ignorado
#   theme_example = TRUE, # Se copia ou nao um exemplo do tema, caso ele exista
#   empty_dirs = FALSE, # Se mantem os diretorios gerados pelo Hugo vazios
#   to_yaml = TRUE, # Se converte os metadados de todos os posts para YAML
#   serve = interactive() # Se inicial um servidor local para servir o site
# )
```


```{r}

# 1 - arquivo config.toml gerencia as configuracoes globais do site

# 2 - a pasta content armazena os posts e paginas do site

# 3 - a pasta public contem o site propriamente dito, ela deve ser usada para por o site no ar

```

![](/post/2020-03-17-notas-pessoais-sobre-o-blogdown_files/juiz.jpg)

# 1.3 - RStudio Addins

O RStudio tem alguns Addins ali em cima para ajudar a programar o site em blogdown.

* Server Site - Esse comando precisa ser rodado só uma vez, e faz o seu site recarregar toda vez que voce modifica-lo. Ele será mostrado na parte de View do RStudio, mas eu acho melhor visualizar no browser para trabalhar lado a lado com o código.

* New Post - Cria um novo post, é bem intuitivo... (teste ele para ver o que acontece!)

* Update Metadata - 


* Dá pra criar o projeto do site clicando em `File -> New Project -> New Directory -> Website using blogdown`. Caso isso seja feito, é importante que você vá em `Tools -> Project Options -> Build Tools` e desmarque as opções `Preview site after building` e `Re-knit current preview when supporting files change`.

# 1.4 - Opções globais

Esse capítulo não é muito útil, ele fala de algumas micro configurações que podem ajudar a facilitar a manutenção do site, mas são tão difíceis de aplicar que é melhor ter um pouquinho mais de trabalho mesmo.

# 1.5

# To do list
* Como colocar o bookdown ou alguma configuracao de output especifica no blogdown
* Como deixar o texto justificado no site
* Como escrever do lado de um plot do ggplot

![](/post/2020-03-17-notas-pessoais-sobre-o-blogdown_files/juiz.jpg)

<iframe width="560" height="315" src="https://www.youtube.com/embed/eFyAyz6Xy6g" frameborder="0" allowfullscreen></iframe>











